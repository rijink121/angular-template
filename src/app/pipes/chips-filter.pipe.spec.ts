import { ChipsFilterPipe } from './chips-filter.pipe';

describe('ChipsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChipsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
