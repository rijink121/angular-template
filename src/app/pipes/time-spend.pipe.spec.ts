import { TimeSpendPipe } from './time-spend.pipe';

describe('TimeSpendPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeSpendPipe();
    expect(pipe).toBeTruthy();
  });
});
